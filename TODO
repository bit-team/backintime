TODO:
* add "expert option": a command to be executed after a new snapshot was taken
* use rsync return code to check that the snapshot was really taken (no out of space, copy error ...)
* add Save/Restore snapshot/snapshots command line (by name or "last")
* add Save/Restore snapshot/snapshots dialog
* Toolbars: add different texts for labels and tooltips
* KDE4: handle command line parameters
* KDE4: handle application raise
* KDE4: add right-click menu
* KDE4: better integration with kde/dolphin
