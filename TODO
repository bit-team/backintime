TODO:
* Add show hidden/backup files in folder view
* Bug fix: it does not include ".*" items  even if they are not excluded.
* Bug fix: I want to include "/var" directory but I want to exclude "/var/cache" and it does not wok.
* add a QT4 GUI
